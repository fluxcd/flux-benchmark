apiVersion: builtin
kind: LabelTransformer
metadata:
  name: labels
labels:
  app.kubernetes.io/part-of: flux
  app.kubernetes.io/instance: flux-system
  app.kubernetes.io/role: testing
fieldSpecs:
  - path: metadata/labels
    create: true
